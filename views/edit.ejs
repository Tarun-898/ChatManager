<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <link rel="stylesheet" href="/style1.css"> 
</head>
<body>
    <div class="edit-container"> 
        <h2>Edit your chat of ID <%=newData.id%></h2>
        <form method="POST" action="/chats/<%=newData._id%>?_method=PUT">
            <div class="form-group"> 
                <textarea name="msg" class="edit-textarea"><%=newData.msg%></textarea>
            </div>
            <div class="form-actions"> 
                <button class="submit-btn">Submit</button>
            </div>
        </form>
    </div>
</body>
</html>
